# Mật khẩu đăng nhập trang quản trị (bắt buộc)
ADMIN_PASSWORD=BacNgheAn2026

# Chuỗi bí mật dùng để ký phiên đăng nhập (tuỳ ý, càng dài càng an toàn)
SESSION_SECRET=doi-chuoi-nay-thanh-mot-chuoi-ngau-nhien-dai

# Chuỗi kết nối Neon Postgres. Khi gắn database Neon vào project qua Vercel Marketplace,
# biến này (DATABASE_URL) được Vercel TỰ ĐỘNG thêm vào — không cần điền tay khi deploy.
# Chỉ cần điền thủ công nếu chạy thử ở máy local: copy từ Neon Dashboard > Connection Details,
# hoặc chạy `vercel env pull .env.local` sau khi đã kết nối database trên Vercel.
DATABASE_URL=postgresql://user:password@ep-xxxxxxxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
